---
export const prerender = true;
// import {Food} from '../../react/components/Food';	
import Food from "../../components/Food.astro";
import Layout from "../../layouts/Layout.astro";
export function getStaticPaths() {
	return [{params: {nom: "whom"}}, {params: {nom: "into"}}, {params: {nom: "edge"}}];
}

const {nom} = Astro.params;
---

<Layout title="Knoweats">
	<Food />
</Layout>
