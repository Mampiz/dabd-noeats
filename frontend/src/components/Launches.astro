---
import {type MealsResponse} from "../types/api2";

// Correct fetch usage for a GET request without a body
const res2 = await fetch("https://www.themealdb.com/api/json/v1/1/search.php?f=a", {
	method: "GET"
});
//POL GAY
const {meals} = (await res2.json()) as MealsResponse;
console.log(meals);
---

<div class="grid gap-8 sm:grid-cols-2 lg:grid-cols-3">
	{}
</div>
